version: "2"

services:
  apache:
    image: $IMAGE
    environment:
      APACHE_BACKEND_HOST: drupal
    depends_on:
      - drupal
    volumes:
      - codebase:/var/www/html
  drupal:
    image: wodby/drupal:8-7.1
    volumes:
      - codebase:/var/www/html

volumes:
  codebase: